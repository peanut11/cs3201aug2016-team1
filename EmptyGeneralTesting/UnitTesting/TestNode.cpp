#include "stdafx.h"
#include "CppUnitTest.h"
#include "../SPA/Node.h"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace UnitTesting {
	TEST_CLASS(TestNode) {
public:

	TEST_METHOD(TestSample) {
		/*EntityType nodeType = Node::NodeType::nodeTypeProgram;
		int programName = 3;

		Node* node1 = new Node(nodeType, programName);

		Assert::IsTrue(node1->geNodeType()==nodeType);*/
	}
	};
}